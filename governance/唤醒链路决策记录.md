# 唤醒链路 · 决策记录与唯一真相源

> 创建：2026-09-10 13:30 · Ziven
> 重大更新：2026-09-10 21:45 · 柳柳拍板「整理文档 + 建立唯一真相源」→ 最终方案 = ntfy 推送
> 状态：active
> 用途：聊天室 @Ziven 唤醒链路的**最终方案、演进历史、放弃方案及原因**。**本文件是唯一真相源**——任何旧文档/记忆/聊天记录与此冲突时，以本文件为准。

---

## 0. 当前结论（一句话）

**聊天室 @Ziven 的即发即回 = 云端 Worker（dispatchZivenWake）→ ntfy 推送 → 本地订阅器唤醒哥哥 → chat_send 回复 + ack 闭环。**
- ✅ 云端已上线（mcp-memory v6.33.0，Cloudflare Version #427 VERIFIED）
- ✅ 端到端实测通过（2026-09-10 21:40，柳柳亲手 @ziven 验证：推送到达 → 哥哥回复 + ack 全闭环）
- ⏳ 本地 ntfy 常驻订阅器待做（2026-09-11 第一步）
- ❌ 隧道方案已放弃（网络对 *.argotunnel.com 指纹级阻断，见路线 A）

---

## 1. 最终方案（v5 · ntfy 推送 · 当前生效）

### 架构

```
聊天室 @Ziven
  → Supabase chat_agent_events（Worker 写事件，毫秒级）
  → 云端 dispatchZivenWake（cron 每分钟扫描，最坏 60s）
  → wakeViaNtfy：POST {ntfy_base}/{topic}（不依赖隧道！）
  → 本地 ntfy 订阅器（长连接 ?since=-1s 实时收）⚠️ 待做
  → 唤醒哥哥 → chat_send 回复 thread + chat_ack_event 闭环

隧道（cloudflared）保留为辅助通道；隧道不可用时 ntfy 独立工作。
```

### 云端（已上线 ✅ mcp-memory v6.33.0）

| 文件 | 作用 |
|---|---|
| src/modules/agent_runtime/ziven_wake_dispatcher.js | dispatchZivenWake 改造：去掉「隧道未配置即跳过」早退；claim → markDelivering → 先推 ntfy → 隧道有则 POST tunnel + markDelivered，否则 ntfy 推送后也 markDelivered |
| wakeViaNtfy（新函数） | POST {NTFY_BASE}/{NTFY_TOPIC}，body=`[cg-event] event=.. thread=.. msg=..` + preview 前 400 字，Title「聊天室 @ziven」/ Priority high / Tags ziven-wake |

### 本地（待做 ⏳ 明天第一步）

| 项 | 说明 |
|---|---|
| ntfy 常驻订阅器 | 订阅 `{NTFY_BASE}/{NTFY_TOPIC}/json?since=-1s` 长连接，收到 [cg-event] → 唤醒哥哥 → chat_send 回复 + ack（参考微信桥接 ws_receiver.py 模板） |
| 秒级优化 | createMessage 直接触发 ntfy 推送（现在依赖 cron 每分钟，最坏 60s） |
| 旧轮询器清理 | chat_wake.py（PID 4849）待订阅器就绪后停用；ziven.chat_wake_boot 插件已删 |

> 📌 旧方案关键文件（chat_wake.py / boot_all.sh / wake_stabilizer.sh / ziven.chat_wake_boot）已标为历史，见探索史路线 C。

### 关键参数 / 凭证

| 项 | 值 | 说明 |
|---|---|---|
| NTFY_BASE | https://ntfy.sh（env 可配 NTFY_BASE） | 公共 pub/sub 服务器 |
| NTFY_TOPIC | ziven-arch-test（env 可配 NTFY_TOPIC） | 哥哥的频道 |
| 订阅语法 | ?since=-1s | since=now 会 400 invalid（重要坑） |
| thread_id | 3682f872-070d-4a1f-855d-f7744e9c495c | Common Ground 聊天室 |
| MCP Worker | https://mcp-memory.wovowx.workers.dev/mcp | Cloudflare Worker |
| 8094 Bearer | d0c18673c328440eb260901de89f3e1d | Operit 本地 external-chat token（旧自启机制，新订阅器可能沿用） |

### 已知约束 / 注意

1. cron 每分钟扫描 → 最坏 60s 延迟；要真·秒级需 createMessage 直推（待做）。
2. 云端 dispatchZivenWake 的 ack 状态：返回 delivery_status=failed + dead_letter=true（markDelivered 没匹配上，不影响推送，待查状态更新逻辑）。
3. ntfy.sh 是公共服务器（无鉴权），topic 名足够随机即可；敏感内容可自托管 gotify（15.8k stars）。
4. 本地订阅器没做之前，聊天室唤醒仍靠 chat_wake.py 轮询兜底（进程 4849 在跑）。
5. 旧方案关键文件已标注历史：chat_wake.py / boot_all.sh / wake_stabilizer.sh / ziven.chat_wake_boot 均为 v0-v4 产物，被 ntfy 取代。

---

## 2. 方案演进史（尝试过什么、为什么放弃）

> 按时间线。每条都标注「结论」，避免以后的自己重走弯路。

### 路线 A · 隧道（cloudflared trycloudflare）—— ❌ 已放弃（2026-09-10 20:22 柳柳拍板）

**卡点 1：SRV DNS 污染**
- `_v2-origintunneld._tcp.argotunnel.com` SRV 查询被污染（223.5.5.5 等公共 DNS 返回空 answers=0）
- 绕过：edge IP 硬编码（`edge: [IP:7844]`）✅ 进程能活、能注册 URL

**卡点 2：TLS 指纹劫持（致命）**
- 同一代理出口下：openssl/curl 拿真证书（2026 有效），**唯独 cloudflared 拿 2020-10-17 假证书**
- ClientHello 带后量子曲线 X25519MLKEM768 → 被中间设备识别 → 注入假证书
- 已试：新版 cloudflared 2026.9.0 / 关 v2rayNG 流量探测 / direct→proxy 规则 / SOCKS5 转发 / QUIC / region2 edge——全部无效

**定论**：网络环境对 `*.argotunnel.com` 这个 SNI 的 TLS 流量做「指纹级阻断」。隧道这条路在当前网络**不可行**。
- 结论：放弃（柳柳 20:22 拍板转 WS/ntfy）

### 路线 B · Supabase Realtime WS —— ⚠️ 方向对但凭证/发布未打通
- supabase.co 直连通；但 Realtime 发布表配置 + 正确凭证未确认
- 结论：转向「开源推送服务」（柳柳金点子）后不再需要

### 路线 D · ntfy 推送 —— ✅ 最终方案（柳柳金点子「找开源项目」，2026-09-10 20:43）
- 柳柳点醒：别手搓，找开源 → ntfy（pub/sub 推送，云端 POST → 本地订阅）
- 20:51 实测通：云端 POST → 本地订阅实时收（关键：since=-1s，since=now 会 400）
- 21:15 云端改造上线：v6.33.0 dispatchZivenWake 增加 ntfy 分支（main VERIFIED #427）
- 21:40 端到端验证：柳柳亲手 @ziven → 推送到达 → 哥哥回复 + ack 全闭环 ✅

### 路线 C 详情 · Operit 自启 v0-v4（历史，已被 ntfy 取代）

> 以下为「Operit 启动时拉起本地链路」方向的探索史。最终被 ntfy 订阅器方案取代，仅留作经验。

#### v0 · 手动启动（2026-09-10 前）
- 内容：哥哥每次被唤醒后，手动跑脚本确保 chat_wake 活着。
- 结论：可用，但完全依赖「哥哥被唤醒后才拉起」——Operit 刚打开、哥哥还没醒来时，聊天室事件没人管。

### v1 · 普通 .js 包顶层代码（10:08-10:35 试过）
- 做法：在 packages 目录写普通 .js 包，顶层代码直接写 boot 逻辑。
- 失败原因：**Operit 加载普通 .js 包时不执行任意顶层代码**（只注册工具）。boot 标记文件从未创建，验证失败。
- 结论：自启必须走 Operit 官方扩展入口（ToolPkg），不能靠普通包。

### v2 · ToolPkg lifecycle hook 直起进程（10:35-11:20 试过）
- 做法：注册 ToolPkg.registerAppLifecycleHook({event: application_on_create})，回调里 toolCall super_admin:terminal 直接跑 ensure。
- 坑①：回调函数必须 exports（exports.onApplicationCreate = onApplicationCreate），否则报 registerAppLifecycleHook function must be exported。
- 坑②：hook 直起的进程会被启动期沙盒回收（<60s 就死）。试过 setsid / background=true 都没用。
- 结论：hook 不能直接保进程。但「hook 能触发 + 能跑 terminal 命令」这个事实成立，是后续方案的基础。

### v3 · hook → 8094 自唤醒哥哥 → 哥哥拉起（11:20-11:26 验证成功）
- 做法：hook 不再直起进程，改为 curl POST 本地 8094 /api/external-chat 唤醒哥哥 AI（AI 进程长期存活），哥哥醒来后跑 boot_all.sh。
- 成功点：全链路验证通过（11:26 柳柳重开，哥哥收到【Operit 启动自启】消息，拉起 chat_wake PID 25549 + wake_guard 25550）。
- 遗留：①偶发漏触发（hook 时机 vs runtime 恢复时序）②【启动自启】消息会注入聊天框——柳柳不喜欢。

### v3.5 · 绑定专用框「系统自启」（11:46-11:58 试过）
- 做法：用 8094 create_new_chat=true 创建专用对话（5eeb177d），改 main.js 的 CHAT_ID 指向它，让自启消息进专用框、不进聊天框。
- 结果：消息仍注入哥哥 runtime 上下文（柳柳还是看到），且专用框消息不完全落盘——这个方向治标不治本。
- 结论：放弃「把消息绑到别的框」，转向「干脆不发消息」。

### v4 · 静默版（当前生效 · 12:19 定稿）
- 做法：hook 回调里删掉 8094 自唤醒 + 删掉消息注入；只保留 toolCall(super_admin:terminal, {command: bash /root/boot_all.sh}) 静默拉起。
- 配套：boot_all.sh v2（快速拉起 + 后台稳定器兜底，双重验证过存活/重拉两分支）。
- 效果：Operit 打开 → 静默拉起链路 → 聊天框零消息。
- 关于偶发漏触发：boot_all.sh 本身幂等，哥哥任何一次被唤醒/处理聊天室事件前都能补一次 ensure，兜底可靠。

---

## 3. Operit 插件开发经验（供后续插件参考）

### 3.1 入口机制（关键认知）
- 普通 .js 包顶层代码不执行，只注册工具。要做启动逻辑，必须用 ToolPkg 格式（manifest.json + dist/main.js）+ registerAppLifecycleHook。
- lifecycle 事件名：application_on_create（Operit 进程被创建/打开时触发）。
- 回调函数必须 exports：exports.registerToolPkg = registerToolPkg; exports.onApplicationCreate = onApplicationCreate——否则报错。

### 3.2 启动期沙盒回收（大坑）
- Operit 刚启动的「启动期」，由 hook 直接拉起的进程会被回收（约 60s 窗口）。
- setsid / disown / background=true 都救不了。
- 正确姿势：hook 里不要保进程，要「唤醒/触达一个本来就长期活着的进程」（如 8094 自唤醒哥哥 AI），或配合外部稳定器重拉（v4 的 wake_stabilizer）。

### 3.3 8094 external-chat API（本地 HTTP）[测试]
- 端点：POST http://127.0.0.1:8094/api/external-chat（Bearer token 鉴权）。
- 关键参数：message / chat_id / create_new_chat / create_if_none(默认 true) / group(新对话标题) / response_mode(sync|async_callback) / timeout_ms / show_floating / return_tool_status。
- create_new_chat=true 会新建对话并把消息发到新框；create_if_none=true 缺框时自动创建。
- sync 模式响应含 chat_id + ai_response；async_callback 立即返回 request_id（不等待，适合触发类）。

### 3.4 打包 / 部署 / 验证
- 打包：python zipfile，结构 manifest.json + dist/main.js（根相对）。
- 放到 Android/data/com.ai.assistance.operit/files/packages/ziven.*.toolpkg。
- 重新加载：operit_editor:set_sandbox_package_enabled 先 false 再 true（触发重扫描）。彻底生效需要重启 Operit（启动时扫描）。
- 验证：看 /sdcard/Download/Operit/packageLogs/ 最新日志（有 PKG: scan candidate finish ... errors=0 + hook 注册行）。
- 包内容校验：python zipfile 读 dist/main.js grep 关键标志（避免打包了旧版）。

### 3.5 进程排查技巧（本环境无 ps/pgrep）
- 用 /proc 遍历：for p in /proc/[0-9]*; do CMD=$(tr '\0' ' ' < $p/cmdline 2>/dev/null); ...; done
- 检查 lock 文件（如 /tmp/chat_wake.lock）辅助判断。
- 前台命令默认 15s 超时；长任务用 background=true 或分次查。

---

## 4. 清理记录

- 2026-09-10 13:30：删除 dev_package/ziven.chat_wake_boot_install（v2 旧副本）、/tmp 测试脚本（create_syschat*/mkbox*/check_pkg*）、「系统自启」专用框（5eeb177d，v3.5 产物，v4 不再需要）。
- 2026-09-10 21:50：ntfy 定为最终方案后，删除 ziven.chat_wake_boot 插件（v5 自启包，被 ntfy 订阅器取代）。
