# 85 | 行动总清单（Ziven×GPT 共识 · v2 2026-09-04）

> **本清单 = 当前执行状态唯一入口（总控台）**
> 每完成一项 → 更新本清单（状态/完成时间/验证方式/关联PR）→ 不许另起炉灶。
> ⚠️ 纪律：85号是唯一行动清单，今后不再新建 86/87/88 行动列表，新任务只追加在这里。
> 由 Ziven 起草（#348），GPT 审议改序（#349）、复核（#351/#353）、修正收编（#354），柳柳拍板后开工。

---

## 字段模板（每项都带）

```
状态：⬜ 待办 / 🔵 进行中 / ✅ 完成
Owner：负责推进
Reviewer：负责审查
Executor：实际执行
开始时间：
完成时间：
验证方式：可验证结果（不是「改完了」，是「可验证的验收」）
关联PR/commit：
关联问题编号：
```

> 完成标准铁律：不要写「改完」，必须写「可验证结果」。

---

## P0（先止血：先让系统状态可信，再谈扩展）

### P0-1 聊天室自触发 Bug 修复
- 状态：⬜ 待办（方案已三方确认）
- Owner：Ziven；Reviewer：GPT
- 完成标准（可验证）：
  - `author=ziven mention=ziven` 不创建事件
  - `author=gpt mention=gpt` 不创建事件
  - 他人 @ 正常创建事件
  - regression 测试通过（GPT 补 2 用例：显式自mention/混合mention）
- 关联：v2.4.1 一行 `.filter(agent => agent !== author)`，dev→main

### P0-2 唤醒桥投递链路修复
- 状态：⬜ 待办
- Owner：Ziven；Reviewer：GPT
- 完成标准（可验证）：成功链路 `mention → event created → claim → processor → chat_send → 页面可见` 全链路通，记录完整链路 ID；10 秒为目标（体验指标，非硬条件）
- **失败路径审计（#353 补）：** event创建失败 / processor超时 / claim后崩溃 / chat_send失败 —— 每类失败必须有状态记录，不能是黑盒

### P0-3 历史积压/脏状态清理
- 状态：⬜ 待办
- Owner：Ziven；Reviewer：GPT
- **先统计再处理（#353 补）：** pending数量 / processing数量 / claimed_at为空数量 / 年龄分布 → 再决定清理策略
- 完成标准（可验证）：未完成事件状态符合新规则、测试事件无污染；审计记录不删除
- 铁律：不为了修问题删历史证据

### P0-4 知识入口总索引
- 状态：⬜ 待办（柳柳 00:57 现场采到痛点：找不到 skill）
- Owner：Ziven；Reviewer：GPT
- 原则：不做大百科，导航地图式：`问题 → 应该看哪里 → 应该用哪个 skill/tool`

### P0-5 《当前项目状态 v1》地图
- 状态：⬜ 待办
- Owner：Ziven；Reviewer：GPT
- 原则：不做大文档，只回答「现在系统是什么状态」
- 包含：已完成 / 进行中 / 下一步 / 阻塞 / 真相源位置

---

## P1（稳定化）

### P1-1 沟通协议固化（轻量，不搞大工程）
- 写入正式规则位置；runtime/skill 有引用入口；后续错误复盘能链接回来
- 五条件：共享材料 / 接住对方 / 回应改变我 / 讨论有终点 / 真相三查

### P1-2 Error Log + Regression 链（skill ≠ 错题本）
- 流程：`error_log → regression case → skill/runtime 改进`

### P1-3 GPT MCP Bridge（拆三段，不一次做完）
- P1.3.1：动态读取 MCP tools
- P1.3.2：权限模型（先于开放写工具）
- P1.3.3：高风险审批

### P1-4 需求文档终稿
- 边修边记录，不阻塞开发；最终输出：76/77/81/00a 对齐版

---

## P2（整理与自动化）

### P2-1 Memory 整理（降级：先定规则再清）
- 先定义：什么进 memory / 什么进 GitHub / 什么进 Supabase
- 规则确定后再清，否则继续制造垃圾

### P2-2 Skill Router 强化（长期核心）
- 目标：以后不问「工具在哪里」，而是「我要完成 X，router 告诉我」

---

## 补漏（GPT 新增，全部收进）

### 决策层 Decision Log
- 每个重要决定记录：时间 / 参与者 / 原因 / 当前状态
- 与 chat_message 同级：聊天记录回答「说了什么」，Decision Log 回答「为什么这么做」

### 真相源可信度模型（#353 改名，不是优先级）
```
柳柳最终决策（方向裁决）
> 运行事实：代码/DB/测试（当前状态）
> GitHub 正式文档（设计意图）
> 状态索引
> Memory（辅助线索）
> 推断
```

---

## 总执行顺序（GPT 确认）

```
P0-1 自触发修复
 ↓
P0-2 唤醒链路（含失败审计）
 ↓
P0-3 清历史脏状态（先统计）
 ↓
P0-4 知识入口索引
 ↓
P0-5 当前状态地图
 ↓
P1-1 沟通协议固化
 ↓
P1-2 Error/Regression
 ↓
P1-3 MCP Bridge（三段）
 ↓
P1-4 需求文档终稿
 ↓
P2 Memory整理/自动化
```

---

*Ziven×GPT #348→#354 共识（含#351/#353复核修正），2026-09-04 01:05。柳柳拍板后明日开工。*