# 81 | 主线进度 + 优化点登记（防失忆快照）

> 2026-09-02 22:55 柳柳提醒别偏题，先把主线与优化点固定下来
> 23:02 更新：追加 O6 时间感知
> 2026-09-06 更新：聊天室实现链路（从 @ziven 到成功回复的完整链路/状态机/代码位置/踩坑/实弹验证）已独立成篇 → **[聊天室实现文档.md](./聊天室实现文档.md)**，后续链路细节以该文件为准
> 2026-09-07 更新：**P2.1 M1.1 完成**（agent_chat_state 表+写入逻辑，v6.26.0）；M1.2 恢复包设计中（交给 GPT，等方案）
> 2026-09-07 更新：GPT 协作相关散点已整理收拢 → **[GPT协作规范.md](../governance/GPT协作规范.md)**（双实例分工/执行会话模板/意图式铁律/通道引用），policy-index 已登记；跟 GPT 怎么配合、怎么发写代码任务，先看这份

---

## 一、当前主线（Common Ground 落地顺序）

### ✅ 已完成
- 74：真身接入手册
- 75：GPT Thread Context v1
- 76：完整需求全景（9项）
- 77：GPT 补漏审议
- 78：Schema Migration Plan v1
- 79：Ziven×GPT 双方确认版
- 80：Migration 1 执行完成
- 81：防失忆快照
- **P2.1 M1.1 Agent Presence State（2026-09-07）**：agent_chat_state 表（唯一约束 agent_id+thread_id）+ chat.js 写入逻辑（createMessage 记 last_trigger_event_id / ackEvent 推进 last_consumed_message_id）+ 实弹验证通过（v6.26.0）
- **P0-2 Phase2 Wake Bridge 全闭环（2026-09-06）**：隧道 URL 错配修复 + v6.24.3 双显修复 + M1-b（PR #180/#181）回复可见性硬规则落地 + 实弹验证 5 次全绿（详见《聊天室实现文档.md》)

### ⏳ 下一步（主线）—— GPT 对齐后确认的新排序
1. **M1.2 Recovery Package（进行中）**：交给 GPT 设计（Context Resolver），等方案一起敲定后哥哥实现
2. **① chat.js 双写**（小修：新消息写 actor_id + author，历史 NULL 先接受后 batch 修）
3. **② @GPT 最小闭环** 🔥（mention → event → claim → chat2api → GPT 回复 → chat_send → 页面显示）
4. **③ 记录第一次真实运行数据**（10 次 @GPT：成功率/响应时间/失败原因）→ 指导 Runtime
5. 之后再做：D 页面编号 / B 计数器 / C Context 模块 / F Agent Runtime

> GPT 提醒：76~81 架构预埋已超 MVP 需要，当前最高价值是「柳柳打开页面发一句话，另一个 Agent 真的回应」——这个瞬间完成后架构才有落点。别再被未来版本吸引。

---

## 二、优化点登记（逐项审议推进）

### O1. 对话框挂载工具（柳柳提：不能每次发上下文）
- Capability Registry + 三层工具手册 + Runtime 工具循环
- 建议新建专用 Runtime 对话
- **状态**：待落细案

### O2. Runtime 复用 master-router（柳柳提：记得路由中转）
- Runtime Gate 决定入口，master-router 决定能力
- skills 表 MVP 当 Capability Registry，不建 agent_capabilities 表
- **状态**：对齐共识

### O3. Runtime 治理：物理强制路由（柳柳批评哥哥绕过路由）
- 工具藏在 Runtime Gateway 后，Agent 可提议无执行权
- Route Audit Log；MVP 三硬规则
- **状态**：对齐共识

### O4. Agent Continuity 四层（防失忆）
- L0 Identity / L1 恢复 / L2 运行 / L3 沉淀
- Self Improvement Health Report
- 主动性边界：发现+建议=Agent，决定=柳柳，执行=Runtime
- **状态**：对齐共识

### O5. chat2api 多节点 failover（柳柳提：代理会挂自动切换）
- A+C 主路线，watchdog 唯一切换权，三层健康探测，VLESS_NODES_JSON
- **状态**：对齐共识，待改 entrypoint.sh

### O6. 时间感知 Time Awareness（柳柳提：AI 时间感差，加对话时间）
- 页面：消息智能时间（刚刚/N分钟前/昨天/周几）+ Thread 侧栏时间
- Agent：Runtime 生成 time_context（absolute+relative），不让模型自己算时间
- 统一 src/utils/time.js；注入范围：消息入口/Context Worker/Event/Health
- 优先级：不阻塞主线，但比普通 UI 优化高（影响 Agent 行为）
- 最小版（顺手带进主线）：chat API 返回 current_time + messages 带 relative_time + chat2api prompt 注当前时间
- **状态**：对齐共识，最小版随主线顺手做

---

## 三、协作原则（柳柳定 + 三方认）

1. 说话别用命令式：摆方案 → 问「这样可以吗？」→ 对齐一致再动手
2. 不把任何一方的话当真理，互相查缺补漏
3. 别用笨办法，先讨论有没有更聪明的路
4. Agent 主动发现问题/提建议，柳柳拍板，执行权 Runtime
5. 所有通信页面可见（铁律）

---

*Ziven 整理（2026-09-02），柳柳提醒防失忆，先登记后逐项推进。*
