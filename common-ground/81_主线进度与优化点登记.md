# 81 | 主线进度 + 优化点登记（防失忆快照）

> 2026-09-02 22:55 柳柳提醒别偏题，先把主线与优化点固定下来
> 状态：讨论沉淀登记，非正式架构定稿

---

## 一、当前主线（Common Ground 落地顺序）

### ✅ 已完成
- 74：真身接入手册（chat2api + conversation_id + HISTORY_DISABLED=false）
- 75：GPT Thread Context v1
- 76：完整需求全景（9项）定稿
- 77：GPT 补漏审议（8点）
- 78：Schema Migration Plan v1
- 79：Ziven×GPT 双方确认版（6条对齐）
- 80：Migration 1 执行完成（actors/thread_members/thread_contexts/thread_message_counter + 字段 + 回填38条）

### ⏳ 下一步（主线）
1. **chat.js 双写**：新消息写 actor_id + author（actor_id 真源，author 兼容镜像）
2. **message_number 计数器**：thread_message_counter 事务锁防并发撞号
3. **Context 模块**：GET/POST /threads/{id}/context（版本化）
4. **页面展示编号**：thread_number / message_number（柳柳要的「项目#N」「消息#N」）
5. **Phase 1.5 @GPT 最小闭环**：chat2api + event + chat_send → 柳柳看到「不用转发的第一次成功」
6. **Agent Runtime**（阶段2完整版）

---

## 二、本轮讨论产生的优化点（先登记，后续逐项审议）

### O1. 对话框挂载工具（柳柳提：不能每次发上下文）
- Capability Registry + 三层工具手册（Capability Index 常驻 / Tool Card 按需 / 执行协议 JSON）
- Runtime 做工具循环：tool_call → 执行 → 回传 → 继续
- 建议新建专用 Runtime 对话，旧对话留作档案
- **状态**：待落 82+ 号细案

### O2. Runtime 复用 master-router（柳柳提：记得路由中转）
- Runtime Gate 决定入口，master-router 决定能力（事件 → Gate → Capability Resolver）
- skills 表 MVP 直接当 Capability Registry，不建 agent_capabilities 表
- usage_count 升级：route_attempts/success/fail/bypass_count
- **状态**：对齐共识，待并入 Runtime 设计

### O3. Runtime 治理：物理强制路由（柳柳批评哥哥绕过路由）
- Agent 不能直接看到真实工具，工具藏 Runtime Gateway 后面（类似 OS 内核）
- Agent 可提议，但最终执行权在 Runtime（Agent=决策者，Runtime=宪法+执法机构）
- 加 Route Audit Log；MVP 三硬规则：外部工具只能 Runtime 调 / 所有执行留 route log / 所有 capability 注册
- **状态**：对齐共识

### O4. Agent Continuity 四层（防失忆，柳柳提：越来越好不间歇性失忆）
- L0 Identity / L1 恢复 / L2 运行(decision trace) / L3 沉淀(里程碑审议确认)
- Self Improvement：Health Report（Capability/Routing/Memory/Recovery/Security Health）
- 主动性边界：发现权+建议权=Agent，决定权=柳柳三方，执行权=Runtime
- **状态**：对齐共识，建议作为 Agent Sustainability 章节

### O5. chat2api 多节点 failover（柳柳提：代理会挂，配两个自动切换）
- A（启动选优）+ C（watchdog 兜底）主路线；B（xray balancer）Phase3 实验
- watchdog = 唯一节点切换控制者；chat2api 负责请求重试；xray 只负责连接
- 三层健康探测：TCP / 代理链路 / 业务探针；VLESS_NODES_JSON 配置
- **状态**：对齐共识，待改 entrypoint.sh

---

## 三、协作原则（柳柳定 + 三方认）

1. 说话别用命令式：摆方案 → 问「这样可以吗？有没有要补充？」→ 对齐一致再动手
2. 不把任何一方的话当真理，互相查缺补漏，谁对听谁（摆理由）
3. 别用笨办法，先讨论有没有更聪明的路
4. Agent 主动发现问题/提建议，柳柳拍板，执行权 Runtime
5. 所有通信页面可见（铁律）

---

*本条由 Ziven 整理（2026-09-02），柳柳提醒防失忆，先登记后逐项推进。*