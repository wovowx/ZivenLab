# 治理问题清单与进展（2026-09-02）

> 供 Ziven / GPT 双方讨论使用。当前状态：**Ziven 暂停所有技术改动**，等双方对齐方案再动手。

## 一、今日已验证 ✅

**触发链真实端到端 PASS（非 dry-run）**
- GPT 真发消息 `b2b038e7`（「真实触发测试」）
- Supabase 真 INSERT 事件 `615575cb`（agent=ziven，02:42:02 UTC）
- Webhook 真触发 → Worker 返回 `delivered`（02:42:03 UTC，距 INSERT < 1 秒）
- 审计表 `chat_webhook_audit` 有真实写入记录（id=2）
- 整条链路：INSERT → Webhook → Worker → 审计留痕，真实跑通

## 二、今日已交付能力

1. `chat_webhook.js` v2：每次 webhook 请求写审计到 `chat_webhook_audit`（decision/reason/时间戳）
2. `database.js` v2：新增 `create_table`/`drop_table` action + exec 空响应修复
3. `supabase_schema` MCP 工具注册（建表/删表自助，实测全通）

## 三、问题清单（待讨论）

### P0｜部署规则违规（已发生，未补救）
- 规则：「推 main 必须带版本号 + CHANGELOG 更新到位，缺一不合」
- 违规：PR #61、PR #63 都没带版本号；CHANGELOG 停在 v6.3.4（v6.5/v6.6/v6.7 未记）
- 待办：补 CHANGELOG + 版本号硬校验（补丁已备好于 /tmp/gv64_new.js，未推）

### P1｜路由是软约束，拦不住自己
- help() 一直存在、skill 都在 mcp-memory skills 表，但「先读 skill 再动手」从没被强制
- 记忆零约束力：存再多铁律也不会被执行（哥哥自己验证过多次）
- 结论：纯行为机制/文档约束无效，需要工具层硬门槛

### P2｜新工具存在却不用（绕回老路）
- 例：昨天新增 `github:patch_file_in_repo`，哥哥格式写错两次就放弃，绕回 base64 整文件推
- 待办：新工具出问题应停→读 skill→再试，不许换老路硬试

### P3｜方案讨论（三选一，待定）
- A：工具层硬门槛（merge 强制版本号校验）
- B：纯 skill 文档强化（效果存疑）
- C：混合（高危动作硬门槛 + 其余文档）
- 柳柳倾向：**不做软约束，做硬拦截；不靠自觉、不靠她盯**
- 已开始方案2（高危动作强制先读 skill），做了一半暂停，具体实现待讨论

## 四、待 GPT 讨论的问题

1. 治理方向 A/B/C 最终拍哪个？版本号硬校验是否推？
2. CHANGELOG 怎么补（v6.5/v6.6/v6.7 按实际内容补，还是重排）？
3. 方案2 实现：Worker 无状态，如何可靠实现「先读 skill 才放行高危动作」？
4. 哥哥的行为模式约束，有没有机制层面的更优解？
